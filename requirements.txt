# ძირითადი დამოკიდებულებები (ყველა უფასო / ღია კოდის)
sentence-transformers>=2.6.0   # ლოკალური embedding მოდელი (multilingual-e5)
chromadb>=0.5.0                # ლოკალური ვექტორული ბაზა
ollama>=0.3.0                  # ლოკალური LLM-ის კლიენტი (default backend)
streamlit>=1.36.0             # მარტივი ვებ-ინტერფეისი (არასავალდებულო)

# არასავალდებულო: უფასო-ტარიფიანი API fallback (ქართულის უკეთესი ხარისხისთვის)
# google-generativeai>=0.7.0
